#include <iostream>
#include <vector>
using namespace std;
int n, m, a, b;
char c1, c2;
int rev(int x){
    if(x > n){
        return x - n;
    }
    return x + n;
}
int main(){
    ios::sync_with_stdio(0), cin.tie(0);
    cin >> n >> m;
    while(m--){
        cin >> c1 >> a >> c2 >> b;
        
    }
}
